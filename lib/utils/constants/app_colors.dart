import 'package:flutter/material.dart';

class AppColors {
  // primary color
  static const Color primary = Colors.blue;

  // normal color
  static const Color white = Colors.white;
  static const Color light = Color.fromARGB(255, 194, 194, 194);
  static const Color extraLight = Color.fromARGB(255, 235, 235, 235);
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color yellow = Colors.amber;
  static const Color favorite = Colors.red;

  static const Color darkgreen = Color.fromARGB(255, 0, 109, 4);

  // widget color

  // icon
  static Color suffixIcon = const Color.fromARGB(255, 200, 200, 200);

  // button
  static const Color button = Color.fromRGBO(198, 124, 78, 1);
  static const Color darkText = Color.fromRGBO(200, 77, 0, 1);

  //
  static const Color bottomNav = Color.fromRGBO(198, 124, 78, 1);

  // home container gredient
  static const Color searchBgContainer = Color.fromRGBO(35, 35, 35, 1);
  static const Color homeContainerF = Color.fromRGBO(17, 17, 17, 1);
  static const Color homeContainerS = Color.fromRGBO(49, 49, 49, 1);

  // splash shadow container
  static const Color shadow = Color.fromARGB(245, 0, 0, 0);

  // bottom nav shadow
  static const Color bottomNavShadow = Color.fromARGB(20, 0, 0, 0);
}
